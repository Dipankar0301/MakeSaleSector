({
    init: function (cmp, event, helper) 
    {
        helper.getOpportunityDetails(cmp, event, helper);
    },
    onSuccess: function (cmp, event, helper) 
    {
        helper.getOpportunityDetails(cmp, event, helper);
    }
});